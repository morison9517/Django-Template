# =============================================================================
# demo/ = 動作確認用のデモページ一式(プロダクトには含まれません)
#
# ▼ このアプリの決まり
#
#   ・DEBUG=True のときだけ INSTALLED_APPS に入る。本番には出ず、
#     デモ用の表(demo_todo)も本番のDBには作られない。
#   ・トップページ("/")は main/urls.py に "" が無いときだけ引き受ける。
#     Djangoは先に書いたURLが勝つので、自分たちのトップページを書いた瞬間に
#     自動で出なくなる(消す作業は不要)。
#   ・いつでも /__demo で開ける。
#   ・★画面は1枚完結。base.html も style.css も使わない。
#     「セットアップが動いているか」を見せる計器なので、共通のファイルを
#     作り替えても壊れないようにしてある。書き方の見本にはしないこと
#     (見本は templates/login.html)。
#
# ▼ 不要になったら、このフォルダと settings.py / urls.py の「デモ」の各3行を消す。
# =============================================================================
